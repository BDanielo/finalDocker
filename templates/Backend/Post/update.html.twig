{% extends 'base.html.twig' %}

{% block title %}
	Modification du post
	{{ post.title }}
	{{ parent() }}
{% endblock %}

{% block body %}
	<section class="container mt-4">
		<h1 class="text-center">Modification du post
			{{ post.title }}</h1>
		{% include 'Backend/Post/_form.html.twig' with {buttonLabel: 'Modifier'} %}
		<a href="{{ path('admin.posts.index') }}" class="btn btn-primary mt-4">Retour à la liste</a>
	</section>
{% endblock %}
